const { getOrders, updateOrder } = require("../../services/store");
Page({ data: { orderId: "", name: "", phone: "", time: "", note: "" }, onLoad(o) { const id = o.id || ""; const order = getOrders().find(item => item.id === id) || {}; this.setData({ orderId: id, name: order.name || "", phone: order.phone || "", time: order.expectedTime || order.time || "", note: order.note || "" }); }, setName(e) { this.setData({ name: e.detail.value }); }, setPhone(e) { this.setData({ phone: e.detail.value }); }, setTime(e) { this.setData({ time: e.detail.value }); }, setNote(e) { this.setData({ note: e.detail.value }); }, save() { if (!this.data.name || !this.data.phone) return wx.showToast({ title: "请填写姓名和电话", icon: "none" }); updateOrder(this.data.orderId, { name: this.data.name, phone: this.data.phone, expectedTime: this.data.time, note: this.data.note }); wx.showToast({ title: "已保存" }); setTimeout(() => wx.navigateBack(), 500); } });
